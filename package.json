{
  "name": "toy-cc-data-layer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Phase 1 数据层（与浏览器原型解耦，可独立 import 与测试）。",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "fake-indexeddb": "^6.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.8"
  }
}
